// Write function to split a string and convert it into an array of words.

function string_to_array(str: string): string[] {
    return str.split(" ")
}

console.log(string_to_array("Robin Hood")); // ["Robin", "Hood"]
